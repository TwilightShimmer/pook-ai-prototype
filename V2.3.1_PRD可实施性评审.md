# POOK AI V2.3.1 PRD 可实施性评审

## 评审对象

本评审将用户提到的两份 PRD 理解为：

1. `/Users/zoran/working/treasure_data/current_version_tasks/V2.3.1/POOK_AI_完整课堂流程引导课_PRD_V0.1.0.md`
2. `/Users/zoran/working/treasure_data/current_version_tasks/V2.3.1/V2.3.1功能PRD_V0.1_POKI状态右栏.md`

POKI 素材：

- `/Users/zoran/working/treasure_data/current_version_tasks/V2.3.1/20260730-174414.png`
- `/Users/zoran/working/treasure_data/current_version_tasks/V2.3.1/20260730-175005.png`
- `/Users/zoran/working/treasure_data/current_version_tasks/V2.3.1/20260730-175011.png`
- 规格：512×512 头像/半身图、1024×1024 高清全身图、512×1024 竖版全身图，均为带透明通道的女孩正面形象

目标代码：

- `/Users/zoran/working/pook-ai-prototype`
- 分支：`v2.3.1`

## 总结论

### 对高保真可交互原型

**足够开始修改，整体支撑度约为 90%。**

两份 PRD 已经足够定义：

- 引导课入口和核心体验目标；
- 引导课的主要环节顺序；
- 上一步、下一步和自动推进的教学意图；
- 问答、选图降级、找积木、拼搭、摄像头展示和分享的主要页面状态；
- POKI 右栏的信息架构；
- POKI 的正常、异常和兜底状态；
- 页面信息分工、文案、动效和验收原则；
- 哪些功能可以在原型中使用手动触发模拟。

因此，可以在不等待正式 ASR、LLM、TTS、摄像头识别和 3D 资源的情况下，制作一套可点击、可评审的 V2.3.1 高保真原型。

### 对正式产品开发

**目前不够。**

正式开发仍缺少计时、状态恢复、异常降级、真实媒体素材、摄像头策略和服务接口等决策。PRD 自身也明确标记为“产品方案初稿/原型验证稿，尚未冻结”。

## 已经足够支撑的原型改造

### 1. 首页引导课入口

- 增加 POKI 漂浮圆形入口；
- 增加首次高光、气泡和“新”状态；
- 点击进入独立引导课；
- 完成后切换为弱化或“再练一次”的演示状态。

### 2. 引导课流程

- 开场与自动推进示范；
- 上一步聚焦教学；
- 下一步提前推进教学；
- 问答 1 的“看—说—选”三态；
- 沉默/不知道时切换到选图；
- 找积木目标观察和计时寻找；
- 固定拼搭的步骤演示；
- 固定作品展示和直接 QA；
- 自由拼搭；
- 作品分享；
- 完成总结。

原型可以使用本地定时器、手动状态切换和模拟成功/失败按钮验证体验，不代表正式技术实现。

### 3. POKI 统一右栏

可以建立统一组件，并覆盖 PRD 指定页面：

- 本节课目标；
- 课前介绍；
- 问答 1；
- 固定拼搭；
- 作品反思；
- 自由拼搭；
- 分享表达；
- 课程完成反馈。

组件可以实现：

- `guide`
- `waiting`
- `observing`
- `ready_to_speak`
- `listening`
- `heard`
- `thinking`
- `speaking`
- `success`
- PRD 中定义的异常和兜底状态

原型中的状态可由页面操作或调试菜单手动切换。

### 4. POKI 素材替换

当前源码中存在：

- 19 处机器人 Emoji；
- 分布于 15 个源码文件；
- 另有见面页运行时生成的 8 个机器人占位形象。

用户已明确这些机器人 Emoji 都是为 POKI 预留的占位符，因此可以统一替换。

推荐不是逐处直接写 `<img>`，而是先建立可复用的 `PokiAvatar` / `PokiCharacter` 组件，再根据使用位置提供：

- 圆形头像；
- 方形头像；
- 半身角色；
- 全身角色；
- 小型提示角色；
- 右栏主角色。

这样后续更换正式 3D 截帧或不同动作素材时，不需要重新修改全部页面。

## POKI 图片的适用性

### 可以直接支持

- 登录引导头像；
- 小队选择页形象；
- 首页漂浮入口；
- 页面切换和教师验证弹层；
- 目标、问答和提示气泡头像；
- 结果页小形象；
- 右栏静态全身或裁切半身展示。

### 状态表达的已确认方案

不要求额外提供不同动作的 POKI 图片。V2.3.1 原型统一采用：

1. POKI 固定形象负责角色身份和在场感；
2. Emoji 状态徽标负责第一层状态识别；
3. 不超过 8 个汉字的短状态标签负责确认含义；
4. 必要时使用轻微呼吸、倾听声波、说话波纹或庆祝弹跳作为辅助；
5. 颜色只用于品牌和层级，不作为状态的唯一识别方式。

推荐状态映射：

| 状态 | Emoji |
|---|---|
| 引导 | 👀 |
| 等待 | ⏳ |
| 观察 | 📷 |
| 轮到孩子说 | 💬 |
| 倾听 | 👂 |
| 听清 | ✅ |
| 思考 | 💭 |
| 回应 | 🔊 |
| 完成 | ⭐ |
| 没听到 | 👂❓ |
| 没听清 | 💬❓ |
| 视觉重试 | 📷❗ |
| 网络异常 | 🔌 |
| 教师暂停 | ⏸️ |

该方案已经足够支撑原型中的全部正常、异常和兜底状态，不再把多套角色动作素材作为原型实施的前置依赖。

## 实施前最好确认的四项

这些问题不会阻止搭建原型框架，但会影响最终内容和验收。

### A. 引导课使用什么具体课程内容

完整流程 PRD 的 D-11 尚未确定主题、时长、积木数量和素材。当前原型已有“管理员的犀牛挑战”相关内容，可以临时复用，但需要产品确认它是否就是引导课内容。

### B. 画布标准

- 当前原型核心比例：`2200 / 1440`
- POKI 右栏 PRD 验收画布：`2200 × 1600`

需要确定 V2.3.1 以哪个尺寸为最终视觉验收标准。未确认前建议保留现有比例，并兼容 2200×1600 检查。

### C. 引导课完成后的入口状态

需要确认：

- 是否强制首次完成；
- 完成后是否保留“再练一次”；
- 中途退出后从头开始还是保留进度。

原型默认可采用：不强制、完成后保留“再练一次”、中途退出从头开始。

### D. POKI 素材导入命名

当前三张素材可以分别稳定命名为：

- `public/assets/poki-avatar-v231.png`
- `public/assets/poki-character-square-v231.png`
- `public/assets/poki-character-full-v231.png`

状态图标直接使用 Emoji，不再要求补充角色动作素材。

## 不阻塞原型、但阻塞正式开发的事项

- 找积木倒计时档位和超时行为；
- 回退后的已找到数量与倒计时恢复；
- 是否允许连续回退；
- 摄像头、QA 和分享是否允许跳过；
- 沉默阈值和提示次数；
- 摄像头权限拒绝、识别失败的正式降级；
- 自由拼搭的完成条件；
- 真实 ASR、LLM、TTS、视觉识别事件；
- 正式 3D 动作名称和资源；
- 视频、拼搭动画和课程内容素材；
- 正式异常文案和埋点阈值。

## 与现有原型的关键关系

- 现有原型已有十步课堂主线、上下步、调试直达、问答、拼搭、分享、成长和奖励结构，可以复用。
- 新 PRD 的“完整课堂流程引导课”应作为独立入口和独立教学流程，不应直接把现有正式课程全部改成新手蒙层。
- 新右栏可以复用现有目标、介绍和问答页面的右侧结构，但需要统一成一个状态组件。
- 见面、找积木和成长卡按 PRD 保持专用布局，不强行加入右栏。
- 引导课要求使用中性人数表达；现有正式课程中的人数选择和角色分工可以保留，但引导课内部不能复用相关多人话术。

## 推荐实施顺序

1. 导入并统一 POKI 素材组件。
2. 建立统一 POKI 状态模型和右栏组件。
3. 将现有指定课堂页面接入统一右栏。
4. 新建独立引导课入口和流程状态机。
5. 加入看—说—选、找积木、拼搭、作品展示和分享的演示状态。
6. 补充调试直达和异常状态演示。
7. 在目标画布和常见横屏尺寸上做视觉验证。
8. 对照两份 PRD 的 P0 清单逐项验收。

## 最终判断

可以开始修改 V2.3.1 原型。建议把本轮目标明确为：

> 完成可点击、可演示、可按 P0 清单评审的高保真原型；真实 AI、语音、摄像头识别和 3D 动作继续使用受控模拟。
